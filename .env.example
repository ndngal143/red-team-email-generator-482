OPENAI_API_KEY=replace_with_your_api_key
# Optional: leave OPENAI_MODEL unset to select an available model at runtime.
PORT=3000
HOST=0.0.0.0
DEMO_BASE_DOMAIN=training.example.internal
TRAINING_BASE_URL=http://localhost:3000
FEEDBACK_FROM_EMAIL=security-awareness@example.internal
# Optional: POST feedback email payloads to an email-provider webhook.
# Leave unset to write feedback notices to data/feedback-notifications.jsonl only.
FEEDBACK_EMAIL_WEBHOOK_URL=
